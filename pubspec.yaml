name: number_format
description: A Flutter package to format numbers
version: 0.0.1
homepage: https://github.com/thisisamank/number_format

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.0.0

flutter:
